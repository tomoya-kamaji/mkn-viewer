# ディレクトリを開くデータフロー: `docs/stock/feature` を開く場合

## 概要
`App.tsx` の `openDirectory()` から `docs/stock/feature` ディレクトリを開き、サイドバーにファイルツリーを表示するまでの完全なデータフロー。

---

## データフロー全体図

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. App.tsx (66行目)                                                         │
│    handleRetry() {                                                          │
│      clearError();                                                           │
│      openDirectory();  ← ここから開始                                        │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. useFileSystem.ts (76-83行目)                                             │
│                                                                             │
│    openDirectory() {                                                        │
│      await withErrorHandling(async () => {                                  │
│        const path = await openDirectoryDialog();  ← ダイアログで選択        │
│        if (path) {                                                          │
│          await openDirectoryFromPath(path);  ← "docs/stock/feature"        │
│        }                                                                     │
│      });                                                                     │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. useFileSystem.ts (50-74行目)                                             │
│                                                                             │
│    openDirectoryFromPath("docs/stock/feature") {                           │
│      updateState({ isLoading: true });  ← ローディング開始                 │
│                                                                             │
│      const result = await withErrorHandling(async () => {                   │
│        // ここからがメイン処理                                              │
│        const fileTree = await scanDirectory("docs/stock/feature");         │
│        const history = addToHistory("docs/stock/feature");                  │
│                                                                             │
│        updateState({                                                        │
│          currentDirectory: "docs/stock/feature",                            │
│          fileTree: [...],  ← ファイルツリー構造                             │
│          history: [...],                                                    │
│          selectedFile: null,                                                │
│          markdownContent: "",                                                │
│          toc: [],                                                            │
│          isLoading: false,                                                   │
│        });                                                                   │
│      });                                                                    │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. lib/tauri.ts (25-27行目)                                                 │
│                                                                             │
│    scanDirectory("docs/stock/feature") {                                   │
│      return await invoke<FileNode[]>(                                       │
│        "scan_directory",                                                    │
│        { path: "docs/stock/feature" }                                      │
│      );  ← Tauri IPC で Rust バックエンドに送信                             │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              │ Tauri IPC (プロセス間通信)
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. src-tauri/src/main.rs (14-19行目)                                        │
│                                                                             │
│    tauri::Builder::default()                                                │
│      .invoke_handler(tauri::generate_handler![                             │
│        scan_directory,  ← コマンドハンドラー登録                             │
│        ...                                                                   │
│      ])                                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 6. src-tauri/src/commands.rs (56-71行目)                                     │
│                                                                             │
│    #[tauri::command]                                                         │
│    pub fn scan_directory(path: String) -> CommandResult<Vec<FileNode>> {    │
│      let root_path = Path::new(&path);  // "docs/stock/feature"            │
│                                                                             │
│      if !root_path.exists() {                                               │
│        return Err(AppError::FileNotFound(path));                            │
│      }                                                                       │
│                                                                             │
│      if !root_path.is_dir() {                                               │
│        return Err(AppError::ScanError(...));                                │
│      }                                                                       │
│                                                                             │
│      build_file_tree(root_path)  ← ファイルツリー構築                       │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 7. src-tauri/src/commands.rs (94-139行目)                                    │
│                                                                             │
│    fn build_file_tree(root: &Path) -> CommandResult<Vec<FileNode>> {       │
│      let mut entries: Vec<FileNode> = Vec::new();                           │
│                                                                             │
│      // ディレクトリを読み込む                                               │
│      let read_dir = fs::read_dir(root)?;  // "docs/stock/feature"          │
│                                                                             │
│      for entry in read_dir.flatten() {                                      │
│        let path = entry.path();                                             │
│        let name = entry.file_name().to_string_lossy().to_string();         │
│                                                                             │
│        // 無視するディレクトリをスキップ (node_modules, .git など)         │
│        if path.is_dir() && should_ignore_dir(&name) {                       │
│          continue;                                                           │
│        }                                                                     │
│                                                                             │
│        if path.is_dir() {                                                   │
│          // ディレクトリの場合、Markdownファイルを含むかチェック             │
│          if contains_markdown_files(&path) {                                │
│            let children = build_file_tree(&path)?;  ← 再帰的に処理          │
│            entries.push(FileNode {                                          │
│              name,                                                           │
│              path: path.to_string_lossy().to_string(),                       │
│              is_directory: true,                                            │
│              children: Some(children),                                       │
│            });                                                               │
│          }                                                                   │
│        } else if is_markdown_file(&path) {  ← .md / .mdc のみ収集          │
│          entries.push(FileNode {                                            │
│            name,                                                             │
│            path: path.to_string_lossy().to_string(),                         │
│            is_directory: false,                                              │
│            children: None,                                                   │
│          });                                                                 │
│        }                                                                     │
│      }                                                                       │
│                                                                             │
│      // 名前でソート（ディレクトリを先に）                                   │
│      entries.sort_by(|a, b| match (a.is_directory, b.is_directory) {       │
│        (true, false) => std::cmp::Ordering::Less,                           │
│        (false, true) => std::cmp::Ordering::Greater,                        │
│        _ => a.name.to_lowercase().cmp(&b.name.to_lowercase()),             │
│      });                                                                     │
│                                                                             │
│      Ok(entries)  ← FileNode[] を返す                                       │
│    }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              │ FileNode[] を返却
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 8. useFileSystem.ts (57-65行目)                                             │
│                                                                             │
│    updateState({                                                            │
│      currentDirectory: "docs/stock/feature",                                │
│      fileTree: [                                                            │
│        {                                                                    │
│          name: "README.md",                                                  │
│          path: "docs/stock/feature/README.md",                               │
│          isDirectory: false,                                                 │
│          children: None                                                     │
│        },                                                                    │
│        {                                                                    │
│          name: "subdir",                                                     │
│          path: "docs/stock/feature/subdir",                                  │
│          isDirectory: true,                                                  │
│          children: [                                                        │
│            { name: "file.md", path: "...", ... }                            │
│          ]                                                                   │
│        },                                                                    │
│        ...                                                                   │
│      ],                                                                      │
│      history: [...],                                                         │
│      isLoading: false,                                                       │
│    });  ← React の状態更新                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              │ React の再レンダリング
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 9. App.tsx (72-88行目)                                                      │
│                                                                             │
│    <Sidebar                                                                  │
│      fileTree={fileTree}  ← ファイルツリーを props で渡す                    │
│      selectedFile={selectedFile}                                            │
│      currentDirectory={currentDirectory}                                    │
│      history={history}                                                       │
│      onSelectFile={selectFile}                                              │
│      ...                                                                     │
│    />                                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 10. Sidebar.tsx (182-199行目)                                               │
│                                                                             │
│     {activeTab === "files" ? (                                               │
│       <>                                                                     │
│         {fileTree.length > 0 ? (                                             │
│           <div className="flex-1 overflow-y-auto">                           │
│             <div className="p-2">                                           │
│               {currentDirectory && (                                         │
│                 <div className="...">                                        │
│                   {currentDirectory.split("/").pop()}  ← "feature"           │
│                 </div>                                                       │
│               )}                                                              │
│               <FileTree                                                     │
│                 nodes={fileTree}  ← ツリーノード配列を渡す                    │
│                 selectedFile={selectedFile}                                  │
│                 onSelectFile={onSelectFile}                                   │
│               />                                                              │
│             </div>                                                           │
│           </div>                                                             │
│         ) : (                                                                │
│           <div>フォルダを選択してください</div>                                 │
│         )}                                                                    │
│       </>                                                                    │
│     ) : ...}                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 11. FileTree.tsx (11-25行目)                                                │
│                                                                             │
│     export function FileTree({ nodes, selectedFile, onSelectFile }) {        │
│       return (                                                               │
│         <ul className="space-y-0.5">                                         │
│           {nodes.map((node) => (                                             │
│             <FileTreeNode                                                    │
│               key={node.path}                                                │
│               node={node}                                                     │
│               selectedFile={selectedFile}                                     │
│               onSelectFile={onSelectFile}                                     │
│               depth={0}                                                       │
│             />                                                                │
│           ))}                                                                 │
│         </ul>                                                                 │
│       );                                                                      │
│     }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 12. FileTree.tsx (34-116行目)                                               │
│                                                                             │
│     function FileTreeNode({ node, selectedFile, onSelectFile, depth }) {    │
│       const [isExpanded, setIsExpanded] = useState(depth < 2);              │
│       const isSelected = node.path === selectedFile;                         │
│                                                                             │
│       const handleClick = () => {                                            │
│         if (node.isDirectory) {                                              │
│           setIsExpanded(!isExpanded);  ← 展開/折りたたみ                     │
│         } else {                                                             │
│           onSelectFile(node.path);  ← ファイル選択                            │
│         }                                                                     │
│       };                                                                      │
│                                                                             │
│       return (                                                               │
│         <li>                                                                 │
│           <button onClick={handleClick}>                                     │
│             {node.isDirectory ? (                                            │
│               <>                                                              │
│                 <svg>...</svg>  ← フォルダアイコン                            │
│                 <svg>...</svg>                                              │
│               </>                                                             │
│             ) : (                                                            │
│               <>                                                              │
│                 <span />                                                     │
│                 <svg>...</svg>  ← ファイルアイコン                           │
│               </>                                                             │
│             )}                                                                │
│             <span>{node.name}</span>  ← "README.md", "subdir" など          │
│           </button>                                                          │
│                                                                             │
│           {/* 子ノードを再帰的にレンダリング */}                              │
│           {node.isDirectory && isExpanded && node.children && (            │
│             <FileTree                                                        │
│               nodes={node.children}                                          │
│               selectedFile={selectedFile}                                     │
│               onSelectFile={onSelectFile}                                     │
│               depth={depth + 1}                                              │
│             />                                                                │
│           )}                                                                 │
│         </li>                                                                 │
│       );                                                                      │
│     }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## データ構造

### FileNode (TypeScript)
```typescript
interface FileNode {
  name: string;              // "README.md"
  path: string;              // "docs/stock/feature/README.md"
  isDirectory: boolean;      // false
  children?: FileNode[];     // ディレクトリの場合のみ
}
```

### FileNode (Rust)
```rust
#[derive(Debug, Serialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct FileNode {
    pub name: String,
    pub path: String,
    pub is_directory: bool,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub children: Option<Vec<FileNode>>,
}
```

---

## 重要なポイント

1. **エラーハンドリング**: `withErrorHandling` でラップされ、エラーは `ErrorContext` に設定される
2. **ローディング状態**: `isLoading: true` → 処理完了 → `isLoading: false`
3. **履歴管理**: `addToHistory()` で開いたディレクトリを履歴に追加
4. **フィルタリング**: `.md` / `.mdc` ファイルのみ収集、`node_modules` などは無視
5. **再帰処理**: ディレクトリは再帰的に走査され、ツリー構造を構築
6. **ソート**: ディレクトリを先に、その後ファイルを名前順でソート
7. **React の状態更新**: `updateState()` で状態を更新すると自動的に再レンダリング

---

## 実際の処理例: `docs/stock/feature` を開く場合

### ディレクトリ構造
```
docs/stock/feature/
├── README.md
├── subdir/
│   └── file.md
└── another.md
```

### 生成される FileNode[]
```json
[
  {
    "name": "subdir",
    "path": "docs/stock/feature/subdir",
    "isDirectory": true,
    "children": [
      {
        "name": "file.md",
        "path": "docs/stock/feature/subdir/file.md",
        "isDirectory": false,
        "children": null
      }
    ]
  },
  {
    "name": "README.md",
    "path": "docs/stock/feature/README.md",
    "isDirectory": false,
    "children": null
  },
  {
    "name": "another.md",
    "path": "docs/stock/feature/another.md",
    "isDirectory": false,
    "children": null
  }
]
```

### サイドバーでの表示
```
📁 subdir ▼
  📄 file.md
📄 README.md
📄 another.md
```

---

## 関連ファイル

- `src/App.tsx` - エントリーポイント
- `src/hooks/useFileSystem.ts` - ファイルシステム状態管理
- `src/lib/tauri.ts` - Tauri API ラッパー
- `src/components/Sidebar.tsx` - サイドバーコンポーネント
- `src/components/FileTree.tsx` - ファイルツリーコンポーネント
- `src-tauri/src/main.rs` - Tauri アプリケーションエントリー
- `src-tauri/src/commands.rs` - Rust コマンド実装


